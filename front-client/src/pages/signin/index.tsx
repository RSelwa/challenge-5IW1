import React from "react"
import { x } from "monorepo"

// import { clientMock, Input } from "monorepo"

const Signin = () => {
  console.log(x)
  // console.log(clientMock)

  return (
    <div>
      sef
      {/* <Input
        id="email"
        label="Email"
        name="email"
        placeholder="Email"
        type="email"
        variant="outline"
        stateRegister="email"
        register={{}}
      /> */}
    </div>
  )
}

export default Signin
